section#intro
	section#intro-intro
		h2 Czym jest JavaScript i czy jest nam potrzebny?
	section
		h4 Gdzie spotkamy JS?
	section
		h4 REST endpoint w Node.js
		pre.fragment.fade-right
			code.javascript.hljs
				| import * as express from 'express';
				| const app = express()
				| const port = 3000
				| 
				| app.get('/', (req, res) => res.send('Hello World!'))
				| 
				| app.listen(port, () => console.log(`Example app listening on port ${port}!`))
